# Used by `npm run dev:emulated` (vite --mode emulated) — points the app at
# the local Firebase Emulator Suite instead of the real chronos-29b82
# project. See src/lib/firebase.js. Run `firebase emulators:start` from
# this directory first.
VITE_USE_EMULATORS=true
