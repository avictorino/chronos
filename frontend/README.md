# Chronos — frontend

🚧 **Planejado, ainda não implementado.**

Este diretório vai abrigar o app Firebase para explorar visualmente o grafo de conhecimento histórico gerado por [`../ingestion`](../ingestion/README.md) — a camada "Google Maps + Wikipedia" da plataforma Chronos.

Nada aqui é funcional ainda; o objetivo deste README por enquanto é só reservar o lugar no monorepo. Quando a implementação começar, este arquivo será substituído pelas instruções reais de setup (framework escolhido, Firebase Hosting/Functions, como apontar para a API de consulta sobre o Postgres).
